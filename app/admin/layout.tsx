import { ReactNode } from "react"

export default function AdminLayout({
  children,
}: {
  children: ReactNode
}) {
  // Layout simples temporário - o layout completo será carregado após login
  return children
}